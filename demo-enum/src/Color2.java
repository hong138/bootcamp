public enum Color2 {
  // enum values (objects)
  RED, BLUE; // 2 objects - static final

  // Normal Class
  // attributes (can be no attributes)
  // constructor
  // method

  // The meaning of enum:
  // JVM will help create the Color Object (RED, BLUE)
  // As user, you cannot create the enum object by yourself
}
